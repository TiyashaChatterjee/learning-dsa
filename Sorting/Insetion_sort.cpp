#include<bits/stdc++>
using namespace std;
int main(){
    int n;
    cout<<"Enter the size of the array: ";
    cin<<n;
    int A[n];
    cout<<"Enter the elements of the array: ";
    for(int i = 0; i<n; i++){
        scanf("%d", &A[i]);
    }
    
    return 0;
}